"use client"

import { useGeneration } from "@/contexts/generation-context"

// Este componente agora é apenas para manter compatibilidade
// O player real é o UnifiedMusicPlayer no layout principal
export function PersistentPlayer() {
  // Não renderiza nada - o UnifiedMusicPlayer cuida de tudo
  return null
}
